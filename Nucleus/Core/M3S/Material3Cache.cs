using Raylib_cs;

namespace Nucleus.Core
{
    /// <summary>
    /// Stores a Material associated with a ModelCache object, this previously had more use when I was planning this out but
    /// the structure changed and simplified as I went along. Keeping it this way just in case.
    /// </summary>
    public class Material3Cache
    {
        public Material Material { get; set; }
    }
}
